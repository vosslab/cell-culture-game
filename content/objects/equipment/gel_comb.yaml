object_name: gel_comb
kind: equipment
label: Gel comb

state_fields:
  - field_name: status
    type: enum
    allowed: [ready]
    default: ready
    description: Standalone gel comb after removal from its cassette.

visual_states:
  status:
    kind: svg
    cases:
      - when: ready
        output: { asset_name: gel_comb }

capabilities: [clickable]

layout:
  default_width: 8
  display_width_cm: 8
  label_width: 6
