object_name: calculation_2_8_ml
kind: decoration
label: 2.8 mL

state_fields:
  - field_name: visible
    type: bool
    default: true
    description: Whether this calculation choice is visible.

visual_states:
  visible:
    kind: svg
    cases:
      - when: true
        output: { asset_name: interpretation_choice_card }
      - when: false
        output: { asset_name: interpretation_choice_card }

capabilities: [clickable]

layout: { default_width: 13, display_width_cm: 17, label_width: 17 }
