# M12 real-consumer capture

## Current replacement-wave evidence

Date: 2026-08-26.

This is one-time evidence from the actual built `dist/` consumer, served by an
ephemeral loopback HTTP server and driven through visible learner UI actions.
It is not a fixture, mock, CSS injection, state injection, or permanent test.
The machine record is
[capture_facts.json](../../figures/svg_visual_quality_m12/capture_facts.json).

`npm run build` completed before this capture: 129 objects, 70 asset specs,
134 SVG entries, 60 DOM-required assets, and 57 scenes. The record contains
six current 1920 x 1080 full-scene captures:

- `bench_basic` (9 placements)
- `hood_basic` (9 placements)
- `cell_counter_basic` (7 placements)
- `microscope_basic` (7 placements)
- `electrophoresis_bench` (16 placements)
- `imaging_bench` (11 placements)

The recorded full-scene, protocol, page, request, console, and render error
lists are empty. The built visible-UI routes reached the solid active whole
P200 state, exact P200 subpart, Trypan candidate plus external rail, and the
viability candidate union with both independently center-owned choices.

Nine one-time grayscale copies cover the six full scenes and the active,
exact-subpart, and Trypan-candidate protocol captures. They preserve the solid
active versus dashed candidate distinction through geometry as well as value.

## Compact-capture boundary

The retained 546 x 307 PNGs and their measurements are earlier one-time
evidence. They were not freshly regenerated after the final build. A new
compact recapture/remeasurement attempt stalled after opening `bench_basic`,
before its ready marker or screenshot; Playwright reported no exception.

This is a disclosed residual evidence gap, not a visual defect: it does not
invalidate the six current full captures or their visible-UI protocol states.
Each scene retains the earlier `compact_left_stage`, `compact_right_stage`,
`compact_left_lower_stage`, `compact_right_lower_stage`, and
`compact_annotation_rail` PNGs for historical compact evidence. Do not promote
their exact sizes, counts, or screenshots to a permanent test contract.

## Cross-package review

A fresh independent cross-package visual review returned **PASS** with no
blocker, high, or medium finding. It found physically credible rounded,
molded, cylindrical, transparent, and functional forms in the full built
consumers; it observed no detached floor shadows. E8's ring-legibility portion
passes: the solid active ring remains distinct from the dashed candidate ring,
including grayscale.

The current human library gallery is
`docs/figures/equipment_kit/review.html`, generated by
`tools/render_svg_library_review.mjs`. Its separate prior `file://` Playwright
proof covered the then-current 135-asset tree with zero broken images. The
current regenerated source gallery contains all 130 live sources. The capture
predates the final ownership repairs; the removed sources included a second full
cassette masquerading as the comb's inserted state, a material-specific MTT vial,
and four standalone binary lead cards. The current tree adds two tank-coordinate
connection overlays.

The gallery uses source-art `<img>` elements, so it is not the shipping-mode
proof. That gap is closed separately by the built production-host review in
`svg_m12_render_mode_review.md`: all 130 current assets,
including all 64 DOM-required forms, passed the real shared host with no load,
mode, namespacing, aspect-ratio, or browser errors. This remains one-time
review evidence rather than a permanent-test target.

## Final gates

The final replacement-wave repository gates passed:

| Gate                                 | Result                                                                   |
| ------------------------------------ | ------------------------------------------------------------------------ |
| Current production build             | 127 objects, 67 asset specs, 130 SVGs, 64 DOM-required assets, 58 scenes |
| Node suite                           | 675 total, 673 passed, 2 skipped                                         |
| `python3 -m pytest tests/ -q`        | 7,662 passed                                                             |
| `npx playwright test --reporter=dot` | 113 passed                                                               |

## Historical full captures

The older M12 compact reachability measurements and frozen-tree screenshots
remain dated historical implementation evidence, not fresh replacement-wave
compact proof.

- [bench](../../figures/svg_visual_quality_m12/bench_basic_full.png)
- [hood](../../figures/svg_visual_quality_m12/hood_basic_full.png)
- [cell counter](../../figures/svg_visual_quality_m12/cell_counter_basic_full.png)
- [microscope](../../figures/svg_visual_quality_m12/microscope_basic_full.png)
- [electrophoresis](../../figures/svg_visual_quality_m12/electrophoresis_bench_full.png)
- [imaging](../../figures/svg_visual_quality_m12/imaging_bench_full.png)

No screenshot or pixel-equivalence regression test was added. Permanent tests
continue to cover durable schemas, semantics, compilation, and real runtime
behavior; captures, contact sheets, inventories, gallery checks, and grayscale
copies remain implementation evidence.
